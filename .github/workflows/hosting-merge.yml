# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy_web_android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v2
        with:
          distribution: 'zulu'
          java-version: '11'
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - run: flutter pub get
      - run: flutter build web
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SYNC_PLAYER_AKM512 }}'
          channelId: live
          projectId: sync-player-akm512
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
      - name: Download release.keystore.jks
        uses: k0staa/download-gdrive-file-action@v1
        with:
          service-account-key-json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SYNC_PLAYER_AKM512 }}
          download-file-name: 'release.keystore.jks'
          download-to: ./android/app/
      - name: Download key.properties
        uses: k0staa/download-gdrive-file-action@v1
        with:
          service-account-key-json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SYNC_PLAYER_AKM512 }}
          download-file-name: 'key.properties'
          download-to: ./android/
      - run: flutter build apk
      - name: Upload app-release.apk
        uses: Jumbo810/Upload_Github_Artifacts_TO_GDrive@v1.1.1
        with:
          target: ./build/app/outputs/flutter-apk/app-release.apk
          credentials: ${{ secrets.UD_SERVICE_ACCOUNT_BASE }}
          parent_folder_id: 1LjXixGDs-_UoGCwY3JBkvByP2uZlIFof